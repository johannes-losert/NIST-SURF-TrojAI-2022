

# use auto method to generate bash script in file called "TEST.sh". Transfer this file via scp or paste it manually. 

#compiler(9,node="juliett",homedir = "testing123", iterations=10,cpus=10,model=3,driver="default", teams =["TrinitySRITrojAI"] ,custom=True, bits = 16)

# EXAMPLE USAGE:
# auto(9,node="pn120395",iterations=10,cpus=10,model=3,driver="default", teams =["TrinitySRITrojAI"] ,custom=True, bits = 16)

#Use the compiler method to create "COMPILER.sh" file required to retrieve results from their respective folders. Paste this into your console or transfer via scp 

# EXAMPLE USAGE (note: "auto" and "compiler" take the same arguments): 

# compiler(9,node="pn120395",iterations=10,cpus=10,model=3,driver="default", teams =["TrinitySRITrojAI"] ,custom=True, bits = 16)



